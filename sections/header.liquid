<h2 class="text-2xl font-semibold text-center text-gray-800 mb-6">{{ shop.name }}</h2>

<div class="flex flex-col items-center space-y-4">
  {% for item in linklists.main-menu.links %}
    <div>
      <a href="{{ item.url }}" class="text-lg font-medium text-blue-600 hover:text-blue-800 hover:underline">
        {{ item.title }}
      </a>
    </div>
  {% endfor %}
</div>
